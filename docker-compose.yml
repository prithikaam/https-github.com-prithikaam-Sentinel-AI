version: '3.8'

services:
  sentinelai:
    build: .
    ports:
      - "5000:5000"
    environment:
      - VITE_API_URL=http://localhost:5000
    volumes:
      - ./backend:/app/backend
    restart: unless-stopped
